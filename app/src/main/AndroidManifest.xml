<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.raveline.estudosnovos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.FloatingActionBarActivity"
            android:label="@string/title_activity_floating_action_bar"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".activities.FragmentsActivity" />
        <activity android:name=".activities.PassandoDadosActivity" />
        <activity android:name=".activities.CardViewActivity" />
        <activity android:name=".activities.RecyclerViewActivity" />
        <activity android:name=".activities.ListViewAcitivity" />
        <activity android:name=".activities.CalculadoraPorcentagem" />
        <activity android:name=".activities.ProgressBarsActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>